function find_average(array) {
  let count = 0;
  if(array.length == 0) {
    return 0
  } else {
    for(i = 0; i < array.length; i++) {
      count += array[i]
    }
    return count / array.length
  }
}
